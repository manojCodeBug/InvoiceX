[package]
name = "payment_manager"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = "21.6.0"

[dev-dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }
invoice_registry = { path = "../invoice_registry" }
