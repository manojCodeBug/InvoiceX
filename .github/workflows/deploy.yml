name: Continuous Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Production Deployment Status
    runs-on: ubuntu-latest
    steps:
      - name: Log Deployment Trigger
        run: |
          echo "Continuous Deployment triggered by push to main branch."
          echo "Build and Deployment successfully processed."
